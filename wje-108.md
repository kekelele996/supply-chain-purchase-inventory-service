<!-- 改编自：美食饮品主题项目提示词/wje-108；后端由 FastAPI (Python) 改造为 Go 1.22 + Gin + GORM -->
# wje-108 餐饮供应链API

请根据以下要求生成项目代码和 README。

## 项目主要需求

### 1. 项目概述与原始功能需求

### 餐饮供应链API — 项目提示词

请根据以下要求生成项目代码。

---

#### 一、硬性约束

##### 1.1 构建可运行性

- 生成的代码必须能直接运行，禁止出现 placeholder、TODO、空实现、`errors.New("not implemented")` 等未完成标记
- 所有函数体必须有完整实现逻辑，不允许空函数或仅 return nil 的函数
- 所有数据库模型必须正确定义字段类型、约束和关系，能通过 GORM AutoMigrate 或 SQL 迁移脚本
- 所有 API 端点必须有完整的请求参数校验和响应序列化逻辑
- 所有 Service 层必须包含完整的业务逻辑实现，包括异常处理和事务管理

##### 1.2 Docker 一键启动

- `docker compose up -d` 一条命令必须能启动全部服务（后端 + MySQL + Redis）
- 所有服务启动后状态必须为 healthy 或 running
- 后端服务必须等待 MySQL 健康检查通过后再启动
- 数据库初始化脚本必须在容器首次启动时自动执行（创建表结构 + 插入测试数据）

##### 1.3 API 功能完整性

- 每个 API 端点必须有完整的 CRUD 操作实现
- 请求参数必须通过 validator/v10 DTO 结构体 严格校验
- 响应格式必须统一（code / message / data 三段式）
- 分页查询必须支持 page / page_size 参数，并返回 total 总数
- 所有列表接口必须支持至少一个筛选条件

##### 1.4 代码质量

- 严禁将多个模块的功能合并到单一文件
- 每个文件的职责必须单一明确
- 函数长度不超过 50 行，超出必须拆分
- 所有异步函数必须正确使用 `async/await`
- 数据库操作必须使用 async session，禁止同步阻塞调用

---

#### 二、项目主要需求

##### 2.1 项目概述

餐饮供应链API是一个面向餐饮企业的后端管理系统，提供供应商管理、食材库存管理、采购订单管理、成本核算和操作日志等核心功能。系统通过 RESTful API 为前端（或第三方系统）提供数据服务，帮助餐饮企业实现供应链的数字化管理。

##### 2.2 核心实体

#### 2.2.1 Supplier（供应商）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer, PK, auto | 主键 |
| name | String(100), unique, not null | 供应商名称 |
| contact_person | String(50), not null | 联系人 |
| phone | String(20), not null | 联系电话 |
| email | String(100), nullable | 邮箱 |
| address | String(200), not null | 地址 |
| categories | JSON, not null | 供应品类列表，如 `["蔬菜","肉类","海鲜"]` |
| rating | Float, default=3.0 | 评级（1.0 ~ 5.0，步长0.5） |
| status | Enum(active/suspended/blacklisted), default=active | 状态 |
| created_at | DateTime, server_default=now() | 创建时间 |
| updated_at | DateTime, onupdate=now() | 更新时间 |

**关系**：供应多个 InventoryItem，关联多个 PurchaseOrder

#### 2.2.2 InventoryItem（食材库存）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer, PK, auto | 主键 |
| name | String(100), not null | 食材名称 |
| category | Enum(vegetable/meat/seafood/seasoning/staple/dry_goods/beverage) | 食材分类 |
| supplier_id | Integer, FK -> suppliers.id | 供应商 |
| batch_no | String(50), unique, not null | 批次号 |
| quantity | Float, not null | 当前余量 |
| unit | String(20), not null | 单位（kg/个/箱/瓶等） |
| min_threshold | Float, default=10.0 | 库存预警阈值 |
| expiry_date | Date, not null | 保质期 |
| storage_location | String(100), not null | 存放位置 |
| status | Enum(normal/low/expired), default=normal | 库存状态 |
| created_at | DateTime, server_default=now() | 入库时间 |

**关系**：属于 Supplier，被多个 PurchaseOrderItem 引用

#### 2.2.3 PurchaseOrder（采购单）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer, PK, auto | 主键 |
| order_no | String(30), unique, not null | 采购单号（自动生成，格式 PO-YYYYMMDD-XXXX） |
| supplier_id | Integer, FK -> suppliers.id | 供应商 |
| status | Enum(draft/pending_approval/approved/rejected/completed), default=draft | 状态 |
| total_amount | Decimal(12,2), default=0 | 总金额 |
| creator_id | Integer, FK -> users.id | 创建人 |
| approver_id | Integer, FK -> users.id, nullable | 审批人 |
| approved_at | DateTime, nullable | 审批时间 |
| completed_at | DateTime, nullable | 完成时间 |
| notes | Text, nullable | 备注 |
| created_at | DateTime, server_default=now() | 创建时间 |

**关系**：属于 Supplier，拥有多个 PurchaseOrderItem，关联 User（creator/approver）

#### 2.2.4 PurchaseOrderItem（采购明细）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer, PK, auto | 主键 |
| order_id | Integer, FK -> purchase_orders.id | 所属采购单 |
| inventory_item_id | Integer, FK -> inventory_items.id | 关联食材 |
| quantity | Float, not null | 采购数量 |
| unit_price | Decimal(10,2), not null | 单价 |
| subtotal | Decimal(12,2), computed | 小计（quantity * unit_price） |

**关系**：属于 PurchaseOrder，引用 InventoryItem

#### 2.2.5 User（用户）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer, PK, auto | 主键 |
| username | String(50), unique, not null | 用户名 |
| password_hash | String(200), not null | 密码哈希（bcrypt） |
| role | Enum(operator/manager/admin), default=operator | 角色 |
| created_at | DateTime, server_default=now() | 创建时间 |

**关系**：创建多个 PurchaseOrder，记录多个 OperationLog

#### 2.2.6 OperationLog（操作日志）

| 字段 | 类型 | 说明 |
|------|------|------|
| id | Integer, PK, auto | 主键 |
| user_id | Integer, FK -> users.id | 操作人 |
| action | Enum(create/update/delete/approve/reject) | 操作类型 |
| target_type | String(50), not null | 操作对象类型（supplier/inventory/purchase_order 等） |
| target_id | Integer, not null | 操作对象ID |
| detail | JSON, nullable | 操作详情（变更前后对比等） |
| created_at | DateTime, server_default=now() | 操作时间 |

**关系**：属于 User

##### 2.3 API 端点列表

#### 认证模块 `/api/v1/auth`
| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | `/login` | 用户登录，返回 JWT token | 公开 |
| POST | `/refresh` | 刷新 token | 需登录 |
| GET | `/me` | 获取当前用户信息 | 需登录 |
| PUT | `/password` | 修改密码 | 需登录 |

#### 用户管理 `/api/v1/users`
| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/` | 用户列表（分页） | admin |
| POST | `/` | 创建用户 | admin |
| GET | `/{id}` | 用户详情 | admin |
| PUT | `/{id}` | 更新用户 | admin |
| DELETE | `/{id}` | 删除用户 | admin |

#### 供应商管理 `/api/v1/suppliers`
| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/` | 供应商列表（分页+筛选：status/category/name） | 需登录 |
| POST | `/` | 新增供应商 | operator+ |
| GET | `/{id}` | 供应商详情 | 需登录 |
| PUT | `/{id}` | 更新供应商 | operator+ |
| DELETE | `/{id}` | 删除供应商（软删除） | admin |
| PUT | `/{id}/status` | 变更供应商状态 | manager+ |
| GET | `/{id}/inventory` | 查看该供应商的库存列表 | 需登录 |
| GET | `/{id}/orders` | 查看该供应商的采购单列表 | 需登录 |

#### 库存管理 `/api/v1/inventory`
| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/` | 库存列表（分页+筛选：status/category/supplier_id/name） | 需登录 |
| POST | `/` | 新增库存（入库） | operator+ |
| GET | `/{id}` | 库存详情 | 需登录 |
| PUT | `/{id}` | 更新库存信息 | operator+ |
| DELETE | `/{id}` | 删除库存记录 | admin |
| GET | `/alerts` | 获取库存预警列表（低于阈值或已过期） | 需登录 |
| PUT | `/{id}/quantity` | 调整库存余量（出库/入库） | operator+ |

#### 采购管理 `/api/v1/purchases`
| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/` | 采购单列表（分页+筛选：status/supplier_id/date_range） | 需登录 |
| POST | `/` | 创建采购单（含明细项） | operator+ |
| GET | `/{id}` | 采购单详情（含明细） | 需登录 |
| PUT | `/{id}` | 更新采购单（仅草稿状态） | operator+ |
| DELETE | `/{id}` | 删除采购单（仅草稿状态） | operator+ |
| PUT | `/{id}/submit` | 提交审批（草稿→待审批） | operator+ |
| PUT | `/{id}/approve` | 审批通过（待审批→已批准） | manager+ |
| PUT | `/{id}/reject` | 审批拒绝（待审批→已拒绝） | manager+ |
| PUT | `/{id}/complete` | 标记完成（已批准→已完成，自动更新库存） | operator+ |

#### 成本统计 `/api/v1/stats`
| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/cost/by-category` | 按食材分类统计采购成本 | 需登录 |
| GET | `/cost/by-supplier` | 按供应商统计采购总额 | 需登录 |
| GET | `/cost/trend` | 按时间段统计成本趋势（日/周/月） | 需登录 |
| GET | `/cost/summary` | 采购成本概览（总采购额、平均单笔、本月采购额） | 需登录 |

#### 操作日志 `/api/v1/logs`
| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | `/` | 操作日志列表（分页+筛选：user_id/action/target_type/date_range） | manager+ |
| GET | `/{id}` | 日志详情 | manager+ |

##### 2.4 横切关注点

#### 2.4.1 认证授权（JWT）

**触达层**：
- **DB**：users 表存储用户信息和密码哈希
- **后端 auth 中间件**：`auth_middleware` 解析 Authorization header，验证 JWT token，将 user 信息注入 request.state
- **后端 auth 路由**：`/api/v1/auth/` 提供登录、刷新 token、获取当前用户等端点
- **DTO 结构体**：`LoginRequest`、`TokenResponse`、`UserResponse` 等认证相关 Schema
- **API 文档安全配置**：在 Go 1.22 + Gin app 中配置 OAuth2PasswordBearer，Swagger UI 显示锁头图标

**实现要求**：
- JWT token 有效期 24 小时，refresh token 有效期 7 天
- 密码使用 bcrypt 哈希存储，禁止明文
- 登录接口需校验用户名和密码，失败返回 401

#### 2.4.2 RBAC 权限控制

**触达层**：
- **DB**：users.role 字段存储角色（operator/manager/admin）
- **后端角色装饰器**：`role_required(*roles)` 装饰器，检查当前用户角色是否在允许列表中
- **路由权限配置**：每个路由通过装饰器声明所需角色
- **采购审批权限检查**：审批操作（approve/reject）必须是 manager 或 admin 角色，且不能审批自己创建的采购单
- **constants 权限枚举**：`RoleEnum` 枚举在 Schema 中统一使用

**角色权限矩阵**：
| 角色 | 供应商 | 库存 | 采购（创建/编辑） | 采购（审批） | 统计 | 日志 | 用户管理 |
|------|--------|------|---------------------|--------------|------|------|----------|
| operator | 查看/创建/编辑 | 查看/创建/编辑 | 创建/编辑/提交/完成 | 无 | 查看 | 无 | 无 |
| manager | 全部操作 | 全部操作 | 全部操作 | 审批/拒绝 | 查看 | 查看 | 无 |
| admin | 全部操作+删除 | 全部操作+删除 | 全部操作 | 审批/拒绝 | 查看 | 查看 | 全部操作 |

#### 2.4.3 操作日志

**触达层**：
- **DB**：operation_logs 表记录所有关键操作
- **后端日志中间件**：`operation_logger` 中间件自动记录写操作（POST/PUT/DELETE）
- **后端日志 service**：`log_service` 提供日志写入和查询功能
- **API 查询接口**：`/api/v1/logs/` 提供日志查询，支持按用户、操作类型、对象类型、时间范围筛选

**记录规则**：
- 所有 POST/PUT/DELETE 请求自动记录
- 记录内容包括：操作人、操作类型、操作对象、变更详情（JSON 格式的 before/after）
- GET 请求不记录
- 登录/登出操作需记录

##### 2.5 共享枚举/常量

#### 2.5.1 PurchaseOrderStatus（采购单状态）

- `draft` — 草稿，可编辑
- `pending_approval` — 待审批，不可编辑
- `approved` — 已批准，等待完成
- `rejected` — 已拒绝，可重新编辑后提交
- `completed` — 已完成，库存已更新

**使用位置**：constants 枚举、数据库字段、API 查询参数、审批流转逻辑

#### 2.5.2 SupplierStatus（供应商状态）

- `active` — 活跃，可正常采购
- `suspended` — 暂停，不可创建新采购单
- `blacklisted` — 黑名单，完全禁止业务往来

**使用位置**：constants 枚举、数据库字段、库存预警逻辑、采购限制校验

#### 2.5.3 InventoryStatus（库存状态）

- `normal` — 正常
- `low` — 低于预警阈值
- `expired` — 已过期

**使用位置**：constants 枚举、数据库字段、预警定时任务、API 响应

#### 2.5.4 InventoryCategory（食材分类）

- `vegetable` — 蔬菜
- `meat` — 肉类
- `seafood` — 海鲜
- `seasoning` — 调料
- `staple` — 主食
- `dry_goods` — 干货
- `beverage` — 饮品

**使用位置**：constants 枚举、数据库字段、成本统计分组

##### 2.6 后端中间件

#### 2.6.1 `auth_middleware` — JWT 认证中间件

- 从 `Authorization: Bearer <token>` header 提取 token
- 验证 token 签名和有效期
- 解码 payload 获取 user_id 和 role
- 将用户信息挂载到 `request.state.user`
- 白名单路径（如 `/api/v1/auth/login`、`/docs`、`/openapi.json`）跳过认证
- token 无效或过期返回 401 响应

#### 2.6.2 `role_required` — 角色权限装饰器

- 装饰器签名：`@role_required("admin", "manager")`
- 从 `request.state.user` 获取用户角色
- 检查角色是否在允许列表中
- 权限不足返回 403 响应
- 与 auth_middleware 配合使用，必须先通过认证

#### 2.6.3 `operation_logger` — 操作日志记录中间件

- 拦截 POST/PUT/DELETE 请求
- 记录操作人（从 request.state.user 获取）
- 根据 URL 路径推断操作对象类型和ID
- 对于更新操作，记录变更前后的数据快照
- 异步写入数据库，不阻塞主请求

#### 2.6.4 `error_handler` — 统一异常处理中间件

- 捕获所有未处理异常，返回统一格式错误响应
- 区分业务异常（BusinessException）和系统异常
- 业务异常返回对应 HTTP 状态码和友好提示
- 系统异常返回 500 并记录日志，不暴露内部信息
- 处理 DTO + validator/v10 校验异常，返回 422 + 字段级错误详情

---


### 2. 核心实体与模块拆分（≥ 4 个实体，必须贯穿后端）

从上面的功能需求中抽象出至少 4 个核心实体。要求：

- 每个实体必须明确字段、关联关系、状态机与业务动作。
- 每个实体必须完整贯穿：数据库表/集合 → `backend/internal/model/<entity>.go` → `backend/internal/dto/<entity>_dto.go` → `backend/internal/repository/<entity>_repository.go` → `backend/internal/service/<entity>_service.go` → `backend/internal/handler/<entity>_handler.go` → `backend/internal/router/<entity>.go` → `backend/internal/constants/<entity>.go`。
- 每个实体的 CRUD 与业务状态流转**严禁合并进单一文件**。
- 多步写操作必须放入 service 层事务中；并发场景必须使用 `SELECT ... FOR UPDATE`（GORM）或 MongoDB 原子操作与唯一索引。


### 3. 核心 API 端点（≥ 6 个，至少 2 个端点复用同一 service/repository）

- 每个端点必须写清楚：方法 + 路径、消费实体、关键交互、复用的 service/repository。
- 路由前缀统一 `/api/v1`，健康检查为 `/healthz`。
- 至少 2 个端点复用同一个 service 方法或 repository 方法，并在端点说明中标注复用关系。


### 4. 横切关注点（≥ 2 个，每个必须写明触达文件层）

至少实现以下 2 项，并逐项写明触达的数据库/后端文件层：

1. **API Key + JWT 双认证 + RBAC 权限**：`internal/middleware/auth.go`、`internal/middleware/rbac.go`、`internal/middleware/api_key.go`、`internal/util/jwt.go`。
2. **操作审计日志与请求追踪**：`internal/middleware/request_id.go`、`internal/middleware/audit.go`、`internal/util/app_error.go`。
3. **接口限流**：`internal/middleware/rate_limiter.go`。


### 5. 共享枚举/常量（≥ 2 组）

至少抽象出 2 组业务状态/类型枚举（如审核状态、风险等级、规则类型、调用方状态）。要求：

- 后端定义位置：`backend/internal/constants/`。
- 同一枚举必须同时出现在：模型、DTO、service 状态机、handler 校验、错误码、日志模板中。
- README 必须列出每个枚举在后端的所有出现位置。


### 7. 后端中间件清单（≥ 2 个）

- 认证中间件、RBAC 权限中间件、请求 ID 中间件、错误恢复中间件、限流中间件、审计日志中间件中至少实现 2 个。
- 每个中间件必须放在 `backend/internal/middleware/` 下的独立文件。

### 8. 技术要求

| 层 | 技术栈 |
| --- | --- |
| 后端 | Go 1.22 + Gin + GORM |
| 数据库 | MySQL 8.0 |
| 缓存 | Redis |
| 认证 | JWT + RBAC |
| 日志 | `log/slog` 或 `go.uber.org/zap` |
| 参数校验 | `github.com/go-playground/validator/v10` |
| 接口文档 | Swagger/OpenAPI |

后端本地开发命令：`cd backend && go mod tidy && go run ./cmd/server`；构建命令：`go build ./...`。

### 9. 文件结构强制清单

```
backend/
├── cmd/server/main.go
├── internal/
│   ├── config/config.go
│   ├── database/database.go
│   ├── model/           # 每个实体一个文件
│   ├── dto/             # 每个实体一个 DTO 文件
│   ├── repository/      # 每个实体一个 repository 文件
│   ├── service/         # 每个实体一个 service 文件
│   ├── handler/         # 每个实体一个 handler 文件
│   ├── router/          # 每个实体一个路由注册文件
│   ├── middleware/
│   ├── constants/
│   └── util/
├── pkg/
├── migrations/
├── Dockerfile
├── go.mod
└── go.sum
database/
├── init.sql 或 migrations
docker-compose.yml
.env
.env.example
README.md
```

**严禁合并职责到单一文件**：不允许把多个实体的 model/repository/service/handler 写进同一个文件，也不允许 main.go 直接写业务逻辑。

### 10. 特殊约束：屎山代码设计（低内聚、高耦合、牵一发动全身）

为了验证模型的跨文件协同改动能力，在规范分层的基础上额外制造合理的“屎山”耦合：

1. **日志模块必须单独管理但全栈引用**
   - 后端必须有一个 `internal/util/logger.go`，使用 `log/slog` 或 `zap` 封装。
   - 所有 handler、service、middleware 都必须引用 logger。
   - 日志格式字符串集中定义在 `internal/constants/log_templates.go`，至少 25 条模板。
   - 业务字段变更必须同步修改对应日志模板和调用处。

2. **异常信息分散且层层透传**
   - 错误码集中在 `internal/constants/error_codes.go`，但每个 service/handler 都要手动拼接 message。
   - handler 必须再次包装 service 返回的错误。
   - 错误 message 中必须包含实体名、字段名、角色名。

3. **常量/工具类多处耦合**
   - `internal/util/formatters.go` 同时包含日期、状态文本、类型文本等格式化逻辑。
   - `internal/constants/messages.go` 同时包含接口返回文案、日志文案、错误提示文案。

4. **状态机必须跨多处定义**
   - 核心状态流转规则在 service 状态机、前端按钮显隐、日志模板、错误码、formatters 中同时存在。
   - 新增一个状态值需要修改至少 10 处。

5. **枚举多处重复定义且被多处引用**
   - 核心枚举必须在后端 constants、DTO、模型、日志模板、错误码、formatters 与前端 constants 中同时存在。
   - 新增一个枚举值必须修改至少 10 处文件。

6. **牵一发动全身验证标准**
   - 后续若要求“给核心业务实体新增一个字段/状态”，必须触达模型、DTO、constants、service、repository、handler、formatters、日志模板、错误码、前端类型与页面等 ≥ 10 个文件。

## Go 企业级工程规范（必须遵循）

后端 Go 代码除满足上述功能与分层要求外，还必须遵循当前 Go 语言通用的企业级工程规范：

### 1. 项目目录规范
- 根目录 `go.mod` 声明 Go 版本 `go 1.22`，模块名使用公司/项目规范（如 `github.com/<org>/<project>`）。
- 采用 `cmd/` + `internal/` + `pkg/` 布局：`cmd/server/main.go` 只负责加载配置、装配依赖、启动服务；私有业务代码全部放在 `internal/`；仅无业务依赖的可复用库放 `pkg/`。
- 数据库迁移脚本放 `migrations/`，接口定义/OpenAPI 文档放 `api/`，Docker/K8s 部署文件放 `deploy/`。
- 包名使用小写单词、不加下划线；文件使用 `snake_case.go`；导出标识符用 `CamelCase`，未导出的用 `camelCase`；禁止使用拼音命名。

### 2. 分层与依赖方向
- 依赖方向严格单向：`handler → service → repository → model`，`model` 不依赖任何上层，禁止反向引用与包循环依赖。
- 每个核心实体按 `model_<entity>.go`、`repository_<entity>.go`、`service_<entity>.go`、`handler_<entity>.go` 分文件，职责单一。
- 依赖通过构造函数注入：`func NewXService(repo Repo, logger *slog.Logger) *XService`；禁止在业务代码中全局 `init` 连接数据库或散落 `sql.Open`。

### 3. 错误处理
- 使用 `fmt.Errorf("list users: %w", err)` 包裹并透传错误，保留错误链。
- 仓储层定义哨兵错误（如 `var ErrNotFound = errors.New("not found")`），上层用 `errors.Is` 判断。
- 禁止在 service 层打印日志后吞掉错误；错误统一由 `middleware` 或 `handler` 转换为标准 JSON 响应。
- 所有 error 必须被处理，关键错误禁止 `_ =` 忽略。

### 4. 配置与日志
- 配置通过环境变量注入，在 `internal/config/config.go` 集中解析，推荐使用 `github.com/caarlos0/env/v11` 或 `spf13/viper`。
- 日志使用 `log/slog` 或 `go.uber.org/zap` 结构化输出；请求日志统一包含 `request_id`、`method`、`path`、`status`、`latency_ms`。

### 5. 参数校验与响应
- 入参校验使用 `github.com/go-playground/validator/v10`，在 DTO 结构体 tag 中声明规则。
- 路由统一 `/api/v1` 前缀；健康检查为 `/healthz`；分页参数统一为 `page`、`page_size`。
- 响应统一为 `{ "code": 0, "message": "ok", "data": ... }` 包裹，错误码集中维护。

### 6. 测试与文档
- 单元测试使用标准库 `testing` + 表驱动（table-driven），文件命名 `*_test.go`；service 与 repository 必须有对应测试。
- 对外接口提供 OpenAPI/Swagger（如 `github.com/swaggo/gin-swagger`）或在 README 中列出完整 API 清单。

### 11. 部署要求

这是纯后端项目，无前端页面/Nginx，通过 API 接口提供服务。

原文端口分配：后端生成 Swagger UI，访问路径：http://localhost:29608/docs；Swagger ReDoc 备用文档：http://localhost:29608/redoc；OpenAPI JSON：http://localhost:29608/openapi.json；访问 http://localhost:29608/docs 能看到 Swagger UI

后端代码放在 `backend/`；数据库脚本放在 `database/`。

项目必须支持 Docker Compose 一键部署，且在 **任意目录名（包括中文目录名）** 下都能正常启动。

需要提供以下文件：
- 根目录 `docker-compose.yml`：编排后端、数据库及所有依赖服务
- 根目录 `.env`：包含 `COMPOSE_PROJECT_NAME=supplychain`
- `backend/Dockerfile`：Go 多阶段构建（见下方骨架）
- `.env.example`：所有环境变量的示例文件，附注释说明；同时包含 `COMPOSE_PROJECT_NAME` 默认值

backend/Dockerfile 使用以下 Go 多阶段构建骨架：
```
FROM golang:1.22-alpine AS builder
WORKDIR /app
COPY go.mod go.sum ./
RUN go mod download
COPY . .
RUN CGO_ENABLED=0 GOOS=linux go build -o /app/server ./cmd/server
FROM alpine:3.20
RUN apk add --no-cache ca-certificates tzdata
WORKDIR /app
COPY --from=builder /app/server .
EXPOSE 8080
CMD ["/app/server"]
```

docker-compose.yml 要求：
- **不要写 `version:` 字段**
- 顶层声明 `name: supplychain`
- 所有服务使用 `container_name` 命名容器，容器名带 `${COMPOSE_PROJECT_NAME:-supplychain}` 前缀
- 数据库数据使用命名卷持久化（`volumes:` 顶层声明，不要使用绑定挂载到中文路径）
- 数据库服务配置 `healthcheck`，后端服务通过 `depends_on: { db: { condition: service_healthy } }` 等待数据库就绪
- 后端服务也配置 `healthcheck`（`/healthz`）
- 暴露端口：后端 `${BACKEND_PORT:-8080}:8080`，数据库 `5432:5432`
- 所有可变配置通过环境变量注入，并在 `.env.example` 中给出默认值
- 支持通过 `docker compose up -d` 一条命令启动全部服务

`.env` / `.env.example` 必须包含的键：
- `COMPOSE_PROJECT_NAME=supplychain`
- 数据库连接相关：`DB_NAME=supplychain_db`、`DB_USER=supplychain_user`、`DB_PASSWORD=supplychain_pwd`
- 后端认证：`JWT_SECRET=change_me_to_a_long_random_string`、`API_KEY_SECRET=change_me_to_a_long_random_string`
- 端口映射：`BACKEND_PORT=8080`、`DB_PORT=5432`
- 缓存连接：`REDIS_ADDR=redis:6379`、`REDIS_PASSWORD=`

## README 写法

根目录必须有 `README.md`，用中文说明项目。

README 包含：
- 项目名称和一句话介绍
- 项目主要功能
- 快速启动方式（Docker Compose 启动命令）
- API 访问地址和 Swagger 文档地址
- 技术栈
- 项目目录结构
- 环境变量说明
- API 调用示例（curl）
- Docker 部署说明（端口映射、数据卷、常见问题）
- License

README 要求：
- Docker 部署命令放在最前面，作为首选启动方式
- 本地开发方式作为备选
- 命令可直接复制运行
- 目录结构用代码块展示
- 技术栈用表格展示，后端一栏写 `Go 1.22 + Gin + GORM`
- 后端本地开发命令为 `cd backend && go mod tidy && go run ./cmd/server`，构建命令为 `go build ./...`
- 提供 curl 调用示例（含 API Key 与 JWT 请求头）
- 不要保留脚手架默认 README 内容
- 必须包含“枚举出现位置清单”，列出所有业务枚举在后端的所有出现位置

## 自检清单（生成代码后必须确认）

1. 在中文目录名下执行 `docker compose config --quiet` 不报错
2. `docker compose up -d` 后所有容器 healthy
3. 访问 `http://localhost:8080/healthz` 返回 200，`/api/...` 接口返回正常，`/swagger/index.html` 可打开
4. README 中的 Docker 启动命令是 `docker compose up -d`
5. 后端在 `backend/` 下执行 `go mod tidy` 与 `go build ./...` 不报错
6. 核心实体 ≥ 4 个，每个实体明确贯穿 model → repository → service → handler → router
7. 核心 API 端点 ≥ 6 个，至少 2 个端点复用同一 service 或 repository
8. 横切关注点 ≥ 2 个，每个写明触达文件层
9. 共享枚举 ≥ 2 组，README 列出所有出现位置
10. 后端中间件 ≥ 2 个
11. 文件结构强制清单已写入
12. 包含“严禁合并职责到单一文件”
13. 包含屎山代码设计要求

